import CreateNotification from '../components/CreateNotification/create.notification';
import React from 'react';

const CreateNotificationPage: React.FC = () => {
    return (
        <CreateNotification />
    );
};

export default CreateNotificationPage;
