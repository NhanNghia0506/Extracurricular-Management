import React from 'react';
import ConfigureAttendance from '../components/ConfigureAttendance/configure.attendance';

const ConfigureAttendancePage: React.FC = () => {
    return (
        <ConfigureAttendance />
    );
};

export default ConfigureAttendancePage;
