import React from 'react';
import ActivityFinalizedParticipants from '../components/ActivityFinalizedParticipants/activity.finalized.participants';

const ActivityFinalizedParticipantsPage: React.FC = () => {
    return (
        <ActivityFinalizedParticipants />
    );
};

export default ActivityFinalizedParticipantsPage;
