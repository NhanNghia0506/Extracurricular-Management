import React from 'react';
import UpdateActivity from '../components/CreateActivity/update.activity';

const UpdateActivityPage: React.FC = () => {
    return (
        <UpdateActivity />
    );
};

export default UpdateActivityPage;
